function greet(name){
  if (name === 'Johnny'){
    return "Hello, my love!";
  } else {
     return "Hello, " + name + "!";
  }
  }
  
  function makeNegative(num) {
    return num < 0 ? num : -num;
  }  

 /* function makeNegative(num) {
    if ( num > 0 ) {
          return -num;
      }
      else {
          return num;
      }
  } */